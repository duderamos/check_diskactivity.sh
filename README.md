check_diskactivity.sh
=====================

NRPE command which returns the level of activity of a partition/volume.

That script gets information from sadf, so you need sysstat installed and enabled in the system.
